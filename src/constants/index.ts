const CONFIG = {
    /* Do not add protocol in endpoint for development phase in case dev server is running locally */
    API_ENDPOINT: 'http://localhost:8000/api/v1',
  };
  
  export default CONFIG;